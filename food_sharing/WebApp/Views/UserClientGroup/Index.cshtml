@model IEnumerable<Domain.UserClientGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Since)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Until)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClientGroup.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Since)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Until)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserClientGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserClientGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserClientGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
