@model IEnumerable<Domain.MenuMeal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Meal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Menu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Meal.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Menu.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MenuMealId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MenuMealId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MenuMealId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
